/**
 * REST controllers for Angular.
 * GameBoardController - api for game board (players in game, moves and etc.)
 * ...
 * ForwardController - needs to replace # from path (work with html5 only).
 */
package ru.ryazanov.ticktacktoe.web.controller;
