/**
 * Jpa Repositories
 * 1. PlayerRepository - work with Player entity. Add findOneByUserName.
 * 2. GameRepository - work with Game entity. Only CRUD.
 * 3. PlayerGameRepository - work with PlayerGame entity. Add findByGame.
 * 4. MoveRepository - work with Move entity. Add findByGame and findByGameAndPlayer.
 */
package ru.ryazanov.ticktacktoe.repository;
